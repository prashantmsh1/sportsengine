[phases.setup]
nixPkgs = ["nodejs_22", "pnpm"]

[phases.install]
cmds = ["pnpm install"]

[phases.build]
cmds = ["pnpm run build --filter=sportsengineapi"]

[start]
cmd = "pnpm run start --filter=sportsengineapi"
